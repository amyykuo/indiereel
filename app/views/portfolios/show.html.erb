<% if current_user.uid == @user.uid %>
  <div class="alert alert-info">
    <h4>This is your <%= @role.role_type %> portfolio , <%= current_user.first_name %>! <small>From here you can see your uploaded works</small></h4>
	</div>
    <h3>Portfolio</h3>
	<%= link_to "Back to your #{@role.role_type} page", role_route(@role) %>
	<!-- this is embedded code
	
	 if !@portfolio.empty? 
	    for media_collect in @media_groups 
  -->
	      <div class="span12">
	        <div class="row-fluid">
	          <div class="span3">
	            <div class="well">
	               <h3></h3>
	                
	                <div class="row-fluid">
	                  <div class="span12">
	                    <div class="nav">
	                    
	                    <h3><%= link_to "View Collection", new_role_path %></h3>
	                    </div>
	                  </div>
	                </div>
	                <hr style="margin:10px 0px 10px 0px" />
	                <div class="btn-group">
                    <%= link_to "Edit", role_route(@role, "edit"), :class => "btn btn-small" %>
                    <%= link_to "Delete", role_route(@role), :class => "btn btn-small" %>
							    </div> 
	            </div>
	          </div>
	        </div>
	      </div>
	      <%= button_to "Add Collection?", custom_new_mc_path %>
  <!-- same as above
	    end 
	 end 
	-->
<% else %>
    <h1>You're on <%= @user.name %>'s <%= @role.role_type %> portfolio!</h1>
	<%= link_to "Back to #{@user.name}'s #{@role.role_type} page", role_route(@role) %>
<% end %>

