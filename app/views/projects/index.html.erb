<% if current_user.uid == @user.uid %>
    <h1>These are the projects you've worked on as a(n) <%= @role %>, <%= current_user.first_name %>!</h1>
<% else %>
    <h1>These are the projects <%= @user.name %> has worked on as a(n) <%= @role %>!</h1>
<% end %>
<%= link_to "Logout", logout_path %>